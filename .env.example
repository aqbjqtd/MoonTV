# =================================================================
# MoonTV 环境变量配置示例
# 复制此文件为 .env 并根据需要修改配置
# =================================================================

# 基础配置
NODE_ENV=production
DOCKER_ENV=true
PORT=3000

# 存储配置 (localstorage | redis | upstash | d1)
STORAGE_TYPE=localstorage

# 认证配置
PASSWORD=admin123  # 必须设置，用于应用认证
USERNAME=admin     # 仅用于非localstorage模式的owner账户

# Redis配置 (当STORAGE_TYPE=redis时)
REDIS_URL=redis://redis:6379
REDIS_PORT=6379

# Upstash配置 (当STORAGE_TYPE=upstash时)
UPSTASH_URL=https://your-upstash-redis-url.upstash.io
UPSTASH_TOKEN=your-upstash-token

# 站点配置
SITE_NAME=MoonTV
NEXT_PUBLIC_SITE_NAME=MoonTV
NEXT_PUBLIC_ENABLE_REGISTER=false
NEXT_PUBLIC_SEARCH_MAX_PAGE=5

# 端口配置
HTTP_PORT=80
HTTPS_PORT=443
PROMETHEUS_PORT=9090
GRAFANA_PORT=3001

# 监控配置
GRAFANA_PASSWORD=admin123

# 性能优化
NODE_OPTIONS=--max-old-space-size=2048

# 安全配置
NEXT_TELEMETRY_DISABLED=1